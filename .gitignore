# Binaries
haul
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Ad-hoc dev binaries / scripts
magnet-test

# Build artifacts
dist/

# Test artifacts
*.test
*.out
coverage.txt
coverage.out

# Go workspace
go.work
go.work.sum

# Config with secrets — use config.example.yaml as template
config.yaml
*.db
*.db-shm
*.db-wal

# Runtime data
data/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code session data
.claude/

# Playwright MCP session artifacts
.playwright-mcp/

# Node
web/ui/node_modules/
web/ui/.vite/
web/ui/coverage/

# Built frontend — embedded into the Go binary via //go:embed.
# Committed so `go build` works without requiring `npm run build` first;
# CI should still run `npm run build` before the Go build to keep it fresh.
# web/static/

# Air (hot reload)
tmp/
.air.toml
