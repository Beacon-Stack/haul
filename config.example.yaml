server:
  host: "0.0.0.0"
  port: 8484

database:
  # path: /config/haul.db  # SQLite file; defaults to /config/haul.db

log:
  level: info
  format: json

# auth:
#   api_key: "your-api-key-here"  # autogenerated on first run if unset

torrent:
  # listen_port: 6881
  # download_dir: ~/Downloads
  # data_dir: /config         # engine-internal state (piece-completion DB); must survive restarts
  # watch_dir: ""             # auto-add .torrent files dropped here; empty disables
  # max_active_downloads: 0   # 0 = unlimited
  # global_download_limit: 0  # bytes/s, 0 = unlimited
  # global_upload_limit: 0
  # default_seed_ratio: 0     # 0 = unlimited
  # default_seed_time: 0      # seconds, 0 = unlimited
  # stall_timeout: 120        # seconds of no data before a torrent counts as stalled
  enable_dht: true
  enable_pex: true
  enable_utp: true
  encryption: prefer  # prefer, require, or disable

# pulse:
#   url: "http://localhost:9696"
#   api_key: "pulse-api-key"
